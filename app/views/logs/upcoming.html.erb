<h3>Upcoming Work</h3>

<% if @current_user_logs.any? %>
  <%= @current_user_logs.each do |log| %>
    <%= link_to  "#{log.category.name} Will be due on #{formatted_date_due(log)}", log_path(log) %><br>
  <% end %>
<% else %>
  <p>Enjoy the break, you don't have anything to do for the next month</p>
<% end %>
